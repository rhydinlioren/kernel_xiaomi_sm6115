This repository does not contain a kernel source tree.

During the build process, the selected upstream kernel source is cloned, the required patches are applied, optional KernelSU integrations are added, and the final AnyKernel3 flashable ZIP is generated.

This keeps the repository lightweight while making it easy to track upstream changes.
